# Loaded by `vite build` (production). The deployed frontend talks to the
# deployed backend. Not a secret — the API is public and unauthenticated.
VITE_API_BASE_URL=https://codesight-code-review-production.up.railway.app
